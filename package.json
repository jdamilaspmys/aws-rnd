{
  "name": "myservice",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "deploy": "ENV=dev sls deploy",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy:local": "ENV=local sls offline start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.752.0"
  },
  "devDependencies": {
    "serverless-offline": "^6.7.0"
  }
}
